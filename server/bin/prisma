#!/bin/sh

# Runs Prisma CLI in a docker container
# Update models:
#  ./bin/prisma deploy --force
docker build -t prisma_cli docker/prisma_cli
echo '----------------'
echo 'Running:'
echo "prisma $@"
echo '----------------'
docker run -v $(pwd):/app prisma_cli prisma $@